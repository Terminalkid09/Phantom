# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environment & secrets
.env
.env.local
*.key
*.pem
*.cert

# Phantom runtime data
data/sessions/*.json
data/cache/
data/downloads/
data/presets/*.json
data/certs/
data/calibrated_weights.json
data/technique_priors.json
data/experience_cases.json

# Self-improvement loop state + beta temp checkouts (machine-authored
# code lives in phantom/automation/guidance/learned/ and IS committed via
# PR review — only the runtime state stays local)
data/evolution/
data/scan_history/
data/reports/
data/beacons/beacon_registry.json
data/phantom_state.json
data/config.json
data/network_hosts.json
# BloodHound/AD graph built from a live engagement (target topology)
data/ad_graph.json
# Social-engineering runtime: persona + conversation state, and the capture
# artefacts generated per target (they name the targets and the lure codes)
data/social_state.json
data/social_attachments/
data/beacons/*.key
data/beacons/*.crt
# C2 audit/runtime state; never commit keys, certificates, or delivery tokens
.data-c2/
data/payload_history.json
data/c2_audit.log.lock

data/logs/
data/engagement_history.json

# Generated persona covers + avatar cache (local, per-engagement)
data/personas/

# Beacon screen recordings + remote-session frames (large binary artifacts)
data/recordings/
data/remote/
# Victim screenshots / camera captures (per-engagement, sensitive)
data/screenshots/

# Stray build artifacts (a toolchain run can create host-named dirs)
/127.0.0.1/
/*.*.*.*/

# Malleable C2 profiles (user-generated, auto-recommended)
data/malleable_profile.json
data/profile_effectiveness.json

# Engagement reports
data/reports/

# Enterprise threat intel cache
data/threatintel_cache/

# CVE / version-lag lookup caches (runtime, regenerated on demand)
data/cve_lookup_cache.json
data/version_lag_cache.json

# Payload artifacts
phantom/payloads/beacon/*.exe
phantom/payloads/beacon/*.exe.tmp
phantom/payloads/beacon/*.o
phantom/payloads/beacon/src/*.o
phantom/payloads/beacon/*.pe
phantom/payloads/beacon/*.bin
phantom/payloads/beacon/*.rva
phantom/payloads/beacon/*.map
phantom/payloads/beacon/*.pdb
phantom/payloads/beacon/*.ilk
phantom/payloads/beacon/*.exp
phantom/payloads/beacon/*.lib

# Reflective loader build artifacts
phantom/payloads/beacon/src/reflective_loader.o
phantom/payloads/beacon/src/reflective_loader.exe
phantom/payloads/beacon/src/reflective_loader.exe.tmp
phantom/payloads/beacon/src/reflective_loader.map
phantom/payloads/beacon/src/reflective_loader.bin
phantom/payloads/beacon/src/reflective_loader_bootstrap.o
phantom/payloads/beacon/src/beacon.exe

# Cross-compiled beacons (all platforms)
phantom/payloads/beacon/beacon_linux
phantom/payloads/beacon/beacon_linux_static
phantom/payloads/beacon/beacon_linux_x86
phantom/payloads/beacon/beacon_macos
phantom/payloads/beacon/beacon_android
phantom/payloads/beacon/beacon_ios.dylib

# Crypto & build config (auto-generated)
phantom/payloads/beacon/src/crypto_config.h
phantom/payloads/beacon/src/c2_config.h
phantom/payloads/beacon/src/beacon_auth.h

# Remote Session module (GUI takeover) build artifacts
phantom/payloads/remote/*.exe
phantom/payloads/remote/remote_linux
phantom/payloads/remote/remote_macos
phantom/payloads/remote/remote.apk
phantom/payloads/remote/remote_ios.dylib
# iOS remote-session build products (Xcode build dir, signing material)
phantom/payloads/remote/ios/build/
phantom/payloads/remote/ios/DerivedData/
phantom/payloads/remote/ios/*.mobileprovision
phantom/payloads/remote/ios/*.p12
phantom/payloads/remote/src/beacon_auth.h
phantom/payloads/remote/src/crypto_config.h
phantom/payloads/remote/src/c2_config.h

# Android remote module (APK) — Gradle/NDK build output + local SDK path
phantom/payloads/remote/android/.gradle/
phantom/payloads/remote/android/build/
phantom/payloads/remote/android/app/build/
phantom/payloads/remote/android/app/.cxx/
phantom/payloads/remote/android/local.properties
phantom/payloads/remote/android/.idea/
phantom/payloads/remote/android/*.iml
phantom/payloads/remote/android/app/src/main/jni/*.o
phantom/payloads/beacon/src/build_id.h
phantom/payloads/beacon/src/config_encrypted.h
phantom/payloads/beacon/src/malleable_config.h
phantom/payloads/beacon/.*.crypto_hash

# Builder output
*_payload.bin

# Windows kernel32.dll copy for VA RVA detection in Docker builds
kernel32.dll.bin
phantom/kernel32.dll.bin

# Temp / test / cache
commands.txt
run_phantom_commands.py
hash_calc.py
comando.txt
*.bak
_test_*.py

# Runtime data
data/*.db
data/*.pid
.pytest_cache/
.pytest_tmp/
report.html
coverage.xml
.coverage
.coverage.*
htmlcov/

# Untracked directories
.antigravitycli/
/payloads/
phantom/data/

# OS artifacts
Thumbs.db
.DS_Store
*.swp
*.swo
*~

# Packet captures
*.pcap
*.pcapng

# Documents
*.pdf
*.xml

# System & temp files
*.log
shell.elf
beacon_debug.log

# .aider
.aider*

# Custom wordlists (generated by wordlist module)
data/wordlists/custom/*.txt
!data/wordlists/custom/.gitkeep
# Bundled wordlists (downloaded at install time, not committed)
data/wordlists/*.txt
data/wordlists/*.lst

# Auto-mode session reports (checkpoints + dual reports per target dir)
data/sessions/auto_*/
data/sessions/auto_*.json
data/sessions/auto_*.md
# Per-target engagement deliverables: data/sessions/report_<target>_<ts>/
# holds client_report.html + raw_audit.html (full engagement content)
data/sessions/report_*/
# Portable .pm session bundles + imported checkpoints (sensitive engagement data)
data/sessions/*.pm
data/sessions/imported_checkpoint.json

# Temp TLS material (built during live-fire beacon tests)
data/tmp_certs/

# Custom persistence rules (generated by persistence module)
# Runtime state (the .sample template is committed)
data/persistence_rules.json
!data/persistence_rules.json.sample

# opencode config
.opencode/

# Private tooling (dynamic generator, polymorphic encoder, stub factory, CLI)
private/

# Electron app
node_modules/
electron/node_modules/
electron/dist/
electron/dist-electron/
electron/dist-release/
electron/.vite/
electron/backend/
!electron/backend/.gitkeep
electron/package-lock.json
*.tsbuildinfo

# Electron runtime state
electron/out/

# Beacon build-test scratch (evasion test artifacts)
aviso/
lab/new_beacon.txt
lab/beacon_client.crt
lab/beacon_client.key
lab/beacon_ca.crt
data/msf_rpc_password
